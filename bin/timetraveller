#!/usr/bin/env node

var program = require('commander');

program
  .version(require('../package.json').version)
  .command('server', 'Run server')
  .command('import', 'Import data')
  .command('create', 'Create map')
  .command('geojson', 'Transform data')
  .parse(process.argv);
